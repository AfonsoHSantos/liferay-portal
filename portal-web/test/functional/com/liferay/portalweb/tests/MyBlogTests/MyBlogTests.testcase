@component-name = "portal-lima"
definition {

	property testray.main.component.name = "Blogs";

	setUp {
		task ("Log in Liferay Portal") {
			TestCase.setUpPortalInstance();

			User.firstLoginPG();
		}

		task ("Create a new page, a blog widget and a blog entry via API") {
			JSONLayout.addPublicLayout(
				groupName = "Guest",
				layoutName = "Blogs Page");

			JSONLayout.addWidgetToPublicLayout(
				column = "1",
				groupName = "Guest",
				layoutName = "Blogs Page",
				widgetName = "Blogs");

			Navigator.gotoPage(pageName = "Blogs Page");

			JSONBlog.addEntry(
				entryContent = "Blogs Entry Content",
				entryTitle = "Blogs Entry Title");
		}
	}

	tearDown {
		var testPortalInstance = PropsUtil.get("test.portal.instance");

		if ("${testPortalInstance}" == "true") {
			PortalInstances.tearDownCP();
		}
		else {
			BlogsEntry.tearDownCP();

			Blogs.tearDownConfigurationDisplaySettingsPG(
				pageName = "Blogs Page",
				portletName = "Blogs");

			JSONLayout.deletePublicLayout(
				groupName = "Guest",
				layoutName = "Blogs Page");

			PagesAdmin.tearDownCP();
		}
	}

	@description = "Can Delete a Blog"
	@priority = "5"
	test CanDeleteBlog {
		task ("create a new blog entry") {
			Blogs.addEntry(
				entryContent = "Blogs Entry Content 2",
				entryTitle = "Blogs Entry Title 2");
		}

		task ("Delete new blog entry") {
			Navigator.gotoSitePage(
				pageName = "Blogs Page",
				siteName = "Guest");

			BlogsEntry.moveToRecycleBinPG(
				entryContent = "Blogs Entry Content 2",
				entryTitle = "Blogs Entry Title 2");
		}

		task ("Validate that the blog no longer exists") {
			Blogs.viewEntryNotPresent(
				entryContent = "Blogs Entry Content 2",
				entryTitle = "Blogs Entry Title 2");
		}

		task ("Validade that the blog that was not moved to the recycle bin still exists"){
			BlogsEntry.viewPG(
				entryContent = "Blogs Entry Content",
				entryTitle = "Blogs Entry Title");
		}
	}

}