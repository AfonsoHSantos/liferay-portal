@component-name = "portal-document-management"
definition {

	property testray.main.component.name = "Document Management";

	setUp {
		task ("Login to the Portal") {
			TestCase.setUpPortalInstance();

			User.firstLoginPG();

			Navigator.openURL();
		}

		task ("Add a New Page") {
			JSONLayout.addPublicLayout(
				groupName = "Guest",
				layoutName = "Documents and Media Page");
		}

		task ("Add DM Widget and navigates to to DM") {
			JSONLayout.addWidgetToPublicLayout(
				column = "1",
				groupName = "Guest",
				layoutName = "Documents and Media Page",
				widgetName = "Documents and Media");

			Navigator.gotoPage(pageName = "Documents and Media Page");
		}

		task ("Create a File in DM by API") {
			JSONDocument.addFileWithUploadedFile(
				dmDocumentDescription = "Resolution Image",
				dmDocumentTitle = "Liferay High Resolution",
				groupName = "Guest",
				mimeType = "image/jpeg",
				sourceFileName = "high_resolution_photo.jpg");
		}
	}

	tearDown {
		var testPortalInstance = PropsUtil.get("test.portal.instance");

		if ("${testPortalInstance}" == "true") {
			PortalInstances.tearDownCP();
		}
		else {
			DMDocument.tearDownCP();

			JSONUser.tearDownNonAdminUsers();

			JSONLayout.deletePublicLayout(
				groupName = "Guest",
				layoutName = "Documents and Media Page");

			PagesAdmin.tearDownCP();

			User.logoutPG();
		}
	}


	@description = "Can Edit a Documents and Media"
	@priority = "5"
	test CanEditDM {
		task ("Go to the DM Page") {
			DMNavigator.openDocumentsAndMediaAdmin(siteURLKey = "guest");
		}

		task ("Edit a DM") {
			DMDocument.editCP(
				dmDocumentDescription = "Resolution Image",
				dmDocumentDescriptionEdit = "DM Document Description Edit",
				dmDocumentFileEdit = "Document_3.jpg",
				dmDocumentFileNameEdit = "Document_3.jpg",
				dmDocumentTitle = "Liferay High Resolution",
				dmDocumentTitleEdit = "DM Document Title Edit");
		}

		task ("Assert new Title and new File") {
			Navigator.gotoPage(pageName = "Documents and Media Page");

			DMDocument.viewEditCP(
				dmDocumentFileNameEdit = "Document_3.jpg",
				dmDocumentTitleEdit = "DM Document Title Edit");
		}
	}

}